import 'package:flutter/material.dart';

/// [Logo] Widget for displaying the logo
class Logo extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Image.asset(
      'assets/image/Logo.png',
      height: 200,
    );
  }
}
